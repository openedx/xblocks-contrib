/* JavaScript for VideoBlock. */

// window.RequireJS = {}
// window.RequireJS.requirejs = requirejs;
// window.RequireJS.require = require;
// window.RequireJS.define = define;

// require.config(
//   {
//     paths: {
//       "jquery": "https://code.jquery.com/jquery-3.7.1.min",
//       "video/00_video_storage": "video/00_video_storage"
//     }
//   }
// );

function VideoBlock(runtime, element) {
  console.log('In VideoBlock.')

  // var storage = VideoStorage('VideoState', id)
  // console.log(storage)

  /*requirejs(
    [
      "video/00_video_storage"
    ],
    function (VideoStorage) {
      var storage = new VideoStorage('VideoState', 0);
      console.log(storage);
    });
*/

  console.log('VideoBlock JS has been loaded.')
}
